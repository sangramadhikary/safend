'use client';

// This file is kept for backward compatibility but is no longer used.
// Property management form is now integrated into FacilityBookingsList.tsx

export function FacilityBookingForm({ onSuccess }: { branchId: string; onSuccess: () => void }) {
  return null;
}
