'use client';

/**
 * @deprecated MealCostManager is no longer used.
 * Per-meal cost is now automatically calculated:
 * Approved Amount / Total Meals = Per Meal Cost
 * See: src/modules/operations/hooks/useMessMealRecords.ts → calculateCharges
 */

export function MealCostManager() {
  return null;
}
